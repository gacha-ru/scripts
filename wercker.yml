box: wercker/golang
build:
  steps:
    -  script:
        name: some simple test!
        code: |-
          go get github.com/go-martini/martini
          go run go/server.go
          curl http://localhost:3000
